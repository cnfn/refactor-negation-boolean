<idea-plugin>
    <id>com.github.cnfn.refactor-code-new</id>
    <name>Refactor Negation Boolean</name>
    <vendor email="cnfnss@gmail.com" url="https://github.com/cnfn">Cnfn</vendor>

    <description><![CDATA[
    Adds some intention to refactor negation boolean check.<br>
    For example:
    <ul>
    <li>if (!x) --> if (BooleanUtils.isFalse(x))</li>
    <li>if (!x) --> if (BooleanUtils.isNotTrue(x))</li>
    <li>if (!StringUtils.isEmpty(str)) --> if (StringUtils.isNotEmpty(str))</li>
    <li>if (!StringUtils.isBlank(str)) --> if (StringUtils.isNotBlank(str))</li>
    <li>if (!CollectionUtils.isEmpty(list)) --> if (CollectionUtils.isNotEmpty(list))</li>
    <li>if (!MapUtils.isEmpty(list)) --> if (MapUtils.isNotEmpty(list))</li>
    </ul>
    ]]></description>

    <!-- please see https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html
         on how to target different products -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.java</depends>


    <extensions defaultExtensionNs="com.intellij">
        <!-- Add your extensions here -->
    </extensions>

    <extensions defaultExtensionNs="com.intellij">
        <intentionAction>
            <className>UseBrotherMethodReplaceNegationBooleanAction</className>
            <category>SDK intentions</category>
        </intentionAction>
        <intentionAction>
            <className>CommonNegationBoolean2NotTrueAction</className>
            <category>SDK intentions</category>
        </intentionAction>
        <intentionAction>
            <className>CommonNegationBooleanAction</className>
            <category>SDK intentions</category>
        </intentionAction>
    </extensions>

    <actions>
        <!-- Add your actions here -->
    </actions>
</idea-plugin>